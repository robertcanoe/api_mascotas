# Capturas para la documentacion

Anade aqui tus capturas de Postman (PNG recomendado). Nombres sugeridos:

- `login-200.png` — POST `/auth/login` con respuesta 200 y token visible (puedes ocultar parte del JWT).
- `get-mascotas.png` — GET `/mascotas` con lista JSON.
- `post-mascotas-admin.png` — POST `/mascotas` con header `Authorization: Bearer ...` y 201.

Despues enlaza las imagenes desde [postman.md](../postman.md).
